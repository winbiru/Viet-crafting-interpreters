nhập gói/chuẩn/main.vi;

hàm main() {
    in nối chuỗi("xin", " chao");
    in nối ba("a", "b", "c");

    in là chuỗi rỗng("");
    in là chuỗi rỗng("x");

    in tất cả(đúng, đúng);
    in tất cả(đúng, sai);
    in một trong hai(đúng, sai);

    ghi thông tin("ok");
    ghi cảnh báo("warn");
    ghi lỗi("err");
};
