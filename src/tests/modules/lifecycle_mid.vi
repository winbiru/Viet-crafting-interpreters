// Import resolution hiện dùng base của compilation context (entry module),
// nên fixture ghi đường dẫn từ src/tests/ để khóa đúng contract hiện tại.
nhập modules/lifecycle_leaf.vi;

in "mid-init";

hàm mid value() {
    trả về leaf value() + 1;
};
